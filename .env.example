# ============================================
# GraphRAG 项目环境变量配置模板
# ============================================
# 使用方法：
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 填写您的实际配置值
# 3. .env 文件已被 .gitignore 忽略，不会提交到代码仓库
# ============================================

# ========== OpenRouter API 配置（必需）==========
# OpenRouter 统一管理所有大模型调用
OPENROUTER_API_KEY=your_openrouter_api_key_here

OPENROUTER_LLM_MODEL=deepseek/deepseek-chat
OPENROUTER_EMBEDDING_MODEL=zhipuai/glm-4-embedding

# ========== 向后兼容配置（已废弃，建议使用 OpenRouter）==========
# 以下配置已废弃，建议迁移到 OpenRouter API
# 如果同时配置了 OpenRouter 和旧配置，将优先使用 OpenRouter
# DEEPSEEK_API_KEY=your_deepseek_api_key_here
# ZHIPU_API_KEY=your_zhipu_api_key_here

# ========== Neo4j 图数据库配置 ==========
# Neo4j 数据库连接信息
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your_neo4j_password_here
# 是否使用加密连接（true/false）
NEO4J_ENCRYPTED=False

# ========== Redis 缓存配置（可选）==========
# Redis 用于缓存对话历史等
REDIS_HOST=0.0.0.0
REDIS_PORT=6379
REDIS_DB=0
# Redis 密码（如果设置了密码）
# REDIS_PASSWORD=your_redis_password_here
REDIS_MAX_CONNECTIONS=10

# ========== 服务端口配置 ==========
# Agent 服务端口（默认：8103）
AGENT_SERVICE_PORT=8103

# Graph 服务端口（默认：8101）
GRAPH_SERVICE_PORT=8101

# Red Spider 服务端口（默认：5001）
RED_SPIDER_SERVICE_PORT=5001

# ========== 其他配置 ==========
# Python 解释器命令（用于启动脚本，默认：python3.11）
# PYTHON_CMD=python3.11
